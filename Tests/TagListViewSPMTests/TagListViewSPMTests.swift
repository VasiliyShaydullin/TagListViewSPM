import XCTest
@testable import TagListViewSPM
import UIKit

final class TagListViewSPMTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TagListViewSPM.tagBackgroundColor, UIColor.gray)
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
